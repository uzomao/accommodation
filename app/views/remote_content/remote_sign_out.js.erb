$("#header").html("<%= escape_javascript(render partial: "shared/navbar") %>");
$("#sessionFlash").html("<%= escape_javascript(render partial: "shared/session_flashes") %>");
console.log("Remote sign out modal should have closed")
console.log("Remote sign out modal should have closed")
console.log("Remote sign out modal should have closed")
console.log("Remote sign out modal should have closed")